syntax = "proto3";

// GroupData is intended to be encapsulated in a UdpPayload message
message GroupData {
    string group_name = 1;
    int64 data_timestamp = 2;
    repeated double double_data = 3;
}
